package game;

/* INTERFACE: The Coordinates interface is a functional interface with only one
 * method.
 */
public interface Coordinates {
	
	/*
	 * Method name is getCoordinates
	 * @param Point [] inShape and boolean xOrY
	 * @return void
	 */
	public void getCoordinates(Point [] inShape);
}
